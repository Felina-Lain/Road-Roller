using UnityEngine;
using System.Collections;

public class reMover : MonoBehaviour {

	//de-cluster the scene, remove trees etc once out of view

	void OnTriggerEnter (Collider col) {
	
		Destroy (col.gameObject);

	}
}
